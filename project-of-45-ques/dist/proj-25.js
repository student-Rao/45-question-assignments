"use strict";
let alien_color = 'yellow' || 'green' || 'red';
alien_color = 'green';
if (alien_color == 'green') {
    console.log("Player earned just 5 point");
}
// Write one version of this program that passes the if test and another that fails. (The version that fails
//  will have no output.)
if (alien_color == 'blue') {
    console.log("Player earned just 5 point");
}
