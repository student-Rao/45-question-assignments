"use strict";
let Username = ["admin", "rao adeel", 'faheem', "rao wakeel"];
if (Username.length == 0) {
    console.log('we found new users');
}
Username.pop();
Username.pop();
Username.pop();
Username.pop();
//Username=[]
if (Username.length == 0) {
    console.log('we found new users');
}
